<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        {% if app.user %}
            <a class="navbar-brand" href="{{ path('home') }}">APPLI FOUR</a>
        {% endif %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
            {% if app.user %}
                <li class="nav-item">
                    <a class="navbar-brand" href="{{ path('recuit') }}">Calcul recuit</a>
                </li>
            {% endif %}
            </ul>
            <ul class="navbar-nav ml-auto">

                {% if app.user %}
                    <li class="nav-item">
                        <a href="#" class="nav-link h5"><i class="fas fa-user-circle"></i> {{ app.user.badge }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="navbar-brand" href="{{ path('logout') }}"> Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="navbar-brand" href="{{ path('login') }}"><i class="fas fa-user-circle"></i> Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>
<div class="container mt-3">
    <h1 class="text-center mt-2 mb-5 p-3"><a id="titre" style="text-decoration: none;" href="{{ path('home') }}">APPLICATION FOUR</a></h1>

    {% for label, messages in app.flashes  %}
    <div class="container">
        <div class="alert alert-{{ label }} text-center">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}